pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.vaadin.com/vaadin-prereleases' }
    }
    plugins {
        id 'com.vaadin' version "$vaadinVersion"
    }
}

rootProject.name = 'hilla-react-gradle-parent'

include 'backend'
include 'webapp'
